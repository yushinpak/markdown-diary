import React from "react";
import styled from "styled-components";

const HomeContainer = styled.div``;

const Home: React.FC = () => {
  return (
    <HomeContainer>
      <p>홈 화면 페이지</p>
    </HomeContainer>
  );
};

export default Home;
